# cpe-mini ↔ cpe-test 对应关系速查

真实项目：**[suikkg/cpe-test](https://github.com/suikkg/cpe-test)**（开源，v6.4.0，
分支 `feat/webui-vue`，92 个 `.rs` / 约 7.6 万行）

**下表的行号对应提交 `4e7e9701d711`**，可以直接拼出永久链接：

```
https://github.com/suikkg/cpe-test/blob/4e7e9701d711f64d86177eb213309c21d72a15d9/<文件路径>#L<行号>
```

例如 `src/verdict.rs:282` →
https://github.com/suikkg/cpe-test/blob/4e7e9701d711f64d86177eb213309c21d72a15d9/src/verdict.rs#L282

**本项目的类型名、字段名、枚举字符串、JSONL 格式都与真实项目一致**，读完这里的代码再去读真实代码不需要二次翻译。

| cpe-mini | cpe-test | 真实规模 |
|---|---|---|
| `src/main.rs` 的 `real_main` + `match mode` | `src/main.rs:40/54/56`（14 个分支） | 260 行 |
| `src/reason.rs` `ReasonCode`（6 个码） | `src/reason.rs:20`（87 个码） | 500 行 |
| `src/verdict.rs` `Verdict` | `src/verdict.rs:21` | — |
| `src/verdict.rs` `ExecutionStatus` | `src/verdict.rs:80` | — |
| `src/verdict.rs` `VerdictResult` | `src/verdict.rs:167` | — |
| `src/verdict.rs` `aggregate_verdict` | `src/verdict.rs:282` | — |
| `ReasonCode::disposition_advice` | `src/verdict.rs:327` | — |
| `src/plan.rs` `Plan` / `validate` | `src/master/plan.rs` + `src/master/webui/plan.rs` | 2197 行 |
| `src/builder.rs` `Unit` | `src/master/builder.rs:247` | 5205 行 |
| `src/builder.rs` `Leg` | `src/master/builder.rs:240` | — |
| 轮次拌进 id | `src/master/builder.rs:742` `round_scoped_id` | — |
| `src/rate_window.rs` | `src/master/rate_window.rs` | 2035 行 |
| `src/executor.rs` | `src/master/executor/`（14 个文件） | 6335 行测试 |
| 判定组装 | `src/master/executor/verdict_assembly.rs:301` | — |
| `src/report.rs` `Row` | `src/report/model.rs:184`（40+ 字段） | — |
| JSONL 落盘 | `src/report/store.rs` | — |

## 真实项目里有、这里刻意没有的

- 起 iperf3 / ctsTraffic 进程，连辅测机（`src/cmd/`、`src/agent/`）
- 网卡扫描与计数器采样（`src/nic/`）
- HTTP 服务和 Vue 前端（`src/master/webui/`、`ui/`）
- Excel / PNG 报告、对比报告（`src/report/chart.rs`、`compare.rs`）
- 断点续跑、取消、超时（`src/cancel.rs`、`run_status.rs`）
- 聚合规则里的两条原因码特例（`is_hard_single_udp_failure`、`blocks_other_legs`）

这些都是**真实工程的复杂度**，不是入门要学的东西。先把主链路吃透。
